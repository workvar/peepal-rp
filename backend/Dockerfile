FROM golang:1.21-alpine AS builder

WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN go build -o collegeerp .

FROM alpine:latest
WORKDIR /app
COPY --from=builder /app/collegeerp .
COPY .env.example .env

EXPOSE 8080
CMD ["./collegeerp"]
