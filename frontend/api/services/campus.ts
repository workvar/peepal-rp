// Campus REST services — all CRUD for hostel, transport, and library
// has been migrated to GraphQL. This file is retained for any future
// REST-only campus endpoints (e.g. file attachments for rooms).

// Bulk upload endpoints (multipart) stay REST — see bulk.ts.
export {};
