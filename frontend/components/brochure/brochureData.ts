// Deprecated. Brochure content now lives in lib/brochure/content.ts (typed
// shape + DEFAULT_CONTENT) and is admin-editable + persisted via GraphQL.
// Kept only as a pointer; safe to delete.
export {};
