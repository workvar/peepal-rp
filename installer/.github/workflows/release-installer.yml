# This path is NOT executed by GitHub Actions (only /.github/workflows/ is).
# The live workflow lives at:
#   .github/workflows/release-installers.yml
#
# Kept here so older docs that link into installer/.github/ still find a pointer.
name: Release installers (moved)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::Installer releases are built by .github/workflows/release-installers.yml at the repo root."
          exit 1
